/* AUTO-GENERATED by opgen — do not edit. */
/* Source: opcodes.def */

#ifndef GEN_INTERP_H
#define GEN_INTERP_H

#include "runtime_api.h"

/* An opcode handler: runs one instruction, then musttail-jumps to
 * its successor (the next opcode, or the trap continuation). */
typedef void (*opcode_handler_t)(vm_t* vm);  /* threaded: TAILs to the next */

/* The 256-entry opcode -> handler table (unfilled entries are NULL). */
const opcode_handler_t* gen_interp_dispatch_table(void);

#endif /* GEN_INTERP_H */
