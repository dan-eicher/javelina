/* Generated by BBQ (C backend) — do not edit */
#include "jnm_reader.h"
#include <stdlib.h>
#include <string.h>

#if defined(__clang__)
#  define BBQ_MUSTTAIL [[clang::musttail]]
#elif defined(__GNUC__) && (__GNUC__ >= 15)
#  define BBQ_MUSTTAIL [[gnu::musttail]]
#else
#  define BBQ_MUSTTAIL
#endif



static bool jnm_n_name_k1(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start);
static bool jnm_n_name_k0(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start);
static bool jnm_n_name_k3(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start);
static bool jnm_n_assoc_k1(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start);
static bool jnm_n_assoc_k3(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start);
static bool jnm_n_assoc_k5(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start);
static bool jnm_n_map_k1(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start);
static bool jnm_n_map_k0(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start);
static bool jnm_n_map_k5(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start);
static bool jnm_n_map_k7(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start);
static bool jnm_n_map_k8(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start);
static bool jnm_n_indirect_assoc_k1(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start);
static bool jnm_n_indirect_assoc_k3(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start);
static bool jnm_n_indirect_assoc_k5(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start);
static bool jnm_n_indirect_map_k1(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start);
static bool jnm_n_indirect_map_k0(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start);
static bool jnm_n_indirect_map_k5(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start);
static bool jnm_n_indirect_map_k7(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start);
static bool jnm_n_indirect_map_k8(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start);
static bool jnm_n_module_name_k0(bbq_ctx_t* ctx, jnm_n_module_name_t* out, size_t _struct_start);
static bool jnm_n_module_name_k3(bbq_ctx_t* ctx, jnm_n_module_name_t* out, size_t _struct_start);
static bool jnm_n_skip_k1(bbq_ctx_t* ctx, jnm_n_skip_t* out, size_t _struct_start);
static bool jnm_n_skip_k0(bbq_ctx_t* ctx, jnm_n_skip_t* out, size_t _struct_start);
static bool jnm_n_subsec_k1(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k0(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k3(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k5(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k13(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k16(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k18(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k20(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k22(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k24(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k26(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k15(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_n_subsec_k28(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start);
static bool jnm_name_data_k2(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start);
static bool jnm_name_data_k3(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start);
static bool jnm_name_data_k5(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start);
static bool jnm_name_data_k6(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start);
bool jnm_n_name_read(bbq_ctx_t* ctx, jnm_n_name_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_name_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_assoc_read(bbq_ctx_t* ctx, jnm_n_assoc_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_assoc_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_map_read(bbq_ctx_t* ctx, jnm_n_map_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_map_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_indirect_assoc_read(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_indirect_assoc_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_indirect_map_read(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_indirect_map_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_module_name_read(bbq_ctx_t* ctx, jnm_n_module_name_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_module_name_k0(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_skip_read(bbq_ctx_t* ctx, jnm_n_skip_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_skip_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_n_subsec_read(bbq_ctx_t* ctx, jnm_n_subsec_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_n_subsec_k1(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
bool jnm_name_data_read(bbq_ctx_t* ctx, jnm_name_data_t* out) {
    // Seed the endian register from the grammar default once at parse start (the
    // CEK seeds little_endian at machine init); the latch makes every later rule
    // call inherit the live register so `@endian:` switches persist.
    if (!ctx->endian_seeded) { ctx->little_endian = true; ctx->endian_seeded = true; }
    bbq_push_loop_base(ctx, bbq_loop_depth(ctx));  // this rule's arrays index from here
    bool _ok = jnm_name_data_k2(ctx, out, bbq_pos(ctx));
    bbq_pop_loop_base(ctx);
    return _ok;
}
static bool jnm_n_name_k1(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->count)) return bbq_fail(ctx, "NName.count: read failed");
    BBQ_MUSTTAIL return jnm_n_name_k0(ctx, out, _struct_start);
}
static bool jnm_n_name_k0(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_bytes(ctx, &out->bytes.data, &out->bytes.length, (size_t)(out->count))) return bbq_fail(ctx, "NName.bytes: read failed");
    BBQ_MUSTTAIL return jnm_n_name_k3(ctx, out, _struct_start);
}
static bool jnm_n_name_k3(bbq_ctx_t* ctx, jnm_n_name_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_assoc_k1(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->idx)) return bbq_fail(ctx, "NAssoc.idx: read failed");
    BBQ_MUSTTAIL return jnm_n_assoc_k3(ctx, out, _struct_start);
}
static bool jnm_n_assoc_k3(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_name_read(ctx, &out->name)) return bbq_fail(ctx, "NAssoc.name: NName failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_assoc_k5(ctx, out, _struct_start);
}
static bool jnm_n_assoc_k5(bbq_ctx_t* ctx, jnm_n_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_map_k1(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->count)) return bbq_fail(ctx, "NMap.count: read failed");
    BBQ_MUSTTAIL return jnm_n_map_k0(ctx, out, _struct_start);
}
static bool jnm_n_map_k0(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    // Counted array: the count bounds the loop, it does not size the store. The
    // backing grows per element exactly as the uncounted path does, so a declared
    // count the input cannot justify costs nothing until the element that runs off
    // the interval — which is where IPG puts the failure (Fig 8 A-Seq/A-Fail; the
    // count appears in no allocation anywhere in the semantics).
    { size_t _n = (size_t)(out->count);
      out->entries.count = 0; out->entries.items = NULL;
      if (_n == 0) { BBQ_MUSTTAIL return jnm_n_map_k8(ctx, out, _struct_start); }
      bbq_push_loop_n(ctx, (int64_t)_n); }
    out->entries.items = bbq_array_grow(ctx, out->entries.items, sizeof(*out->entries.items));
    if (!out->entries.items) return bbq_fail(ctx, "NMap.entries: alloc failed");
    out->entries.count = 1;
    BBQ_MUSTTAIL return jnm_n_map_k5(ctx, out, _struct_start);
}
static bool jnm_n_map_k5(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_assoc_read(ctx, &out->entries.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)])) return bbq_fail(ctx, "NMap.entries.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)]: NAssoc failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_map_k7(ctx, out, _struct_start);
}
static bool jnm_n_map_k7(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (bbq_loop_next(ctx)) {
        out->entries.items = bbq_array_grow(ctx, out->entries.items, sizeof(*out->entries.items));
        if (!out->entries.items) return bbq_fail(ctx, "NMap.entries: array: grow failed");
        out->entries.count = (size_t)(bbq_loop_index(ctx) + 1);
        BBQ_MUSTTAIL return jnm_n_map_k5(ctx, out, _struct_start);
    }
    bbq_pop_loop(ctx);
    BBQ_MUSTTAIL return jnm_n_map_k8(ctx, out, _struct_start);
}
static bool jnm_n_map_k8(bbq_ctx_t* ctx, jnm_n_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_indirect_assoc_k1(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->idx)) return bbq_fail(ctx, "NIndirectAssoc.idx: read failed");
    BBQ_MUSTTAIL return jnm_n_indirect_assoc_k3(ctx, out, _struct_start);
}
static bool jnm_n_indirect_assoc_k3(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_map_read(ctx, &out->map)) return bbq_fail(ctx, "NIndirectAssoc.map: NMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_indirect_assoc_k5(ctx, out, _struct_start);
}
static bool jnm_n_indirect_assoc_k5(bbq_ctx_t* ctx, jnm_n_indirect_assoc_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_indirect_map_k1(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->count)) return bbq_fail(ctx, "NIndirectMap.count: read failed");
    BBQ_MUSTTAIL return jnm_n_indirect_map_k0(ctx, out, _struct_start);
}
static bool jnm_n_indirect_map_k0(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    // Counted array: the count bounds the loop, it does not size the store. The
    // backing grows per element exactly as the uncounted path does, so a declared
    // count the input cannot justify costs nothing until the element that runs off
    // the interval — which is where IPG puts the failure (Fig 8 A-Seq/A-Fail; the
    // count appears in no allocation anywhere in the semantics).
    { size_t _n = (size_t)(out->count);
      out->entries.count = 0; out->entries.items = NULL;
      if (_n == 0) { BBQ_MUSTTAIL return jnm_n_indirect_map_k8(ctx, out, _struct_start); }
      bbq_push_loop_n(ctx, (int64_t)_n); }
    out->entries.items = bbq_array_grow(ctx, out->entries.items, sizeof(*out->entries.items));
    if (!out->entries.items) return bbq_fail(ctx, "NIndirectMap.entries: alloc failed");
    out->entries.count = 1;
    BBQ_MUSTTAIL return jnm_n_indirect_map_k5(ctx, out, _struct_start);
}
static bool jnm_n_indirect_map_k5(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_indirect_assoc_read(ctx, &out->entries.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)])) return bbq_fail(ctx, "NIndirectMap.entries.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)]: NIndirectAssoc failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_indirect_map_k7(ctx, out, _struct_start);
}
static bool jnm_n_indirect_map_k7(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (bbq_loop_next(ctx)) {
        out->entries.items = bbq_array_grow(ctx, out->entries.items, sizeof(*out->entries.items));
        if (!out->entries.items) return bbq_fail(ctx, "NIndirectMap.entries: array: grow failed");
        out->entries.count = (size_t)(bbq_loop_index(ctx) + 1);
        BBQ_MUSTTAIL return jnm_n_indirect_map_k5(ctx, out, _struct_start);
    }
    bbq_pop_loop(ctx);
    BBQ_MUSTTAIL return jnm_n_indirect_map_k8(ctx, out, _struct_start);
}
static bool jnm_n_indirect_map_k8(bbq_ctx_t* ctx, jnm_n_indirect_map_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_module_name_k0(bbq_ctx_t* ctx, jnm_n_module_name_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_name_read(ctx, &out->name)) return bbq_fail(ctx, "NModuleName.name: NName failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_module_name_k3(ctx, out, _struct_start);
}
static bool jnm_n_module_name_k3(bbq_ctx_t* ctx, jnm_n_module_name_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_skip_k1(bbq_ctx_t* ctx, jnm_n_skip_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_bytes(ctx, &out->data.data, &out->data.length, (size_t)(bbq_remaining(ctx)))) return bbq_fail(ctx, "NSkip.data: read failed");
    BBQ_MUSTTAIL return jnm_n_skip_k0(ctx, out, _struct_start);
}
static bool jnm_n_skip_k0(bbq_ctx_t* ctx, jnm_n_skip_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_n_subsec_k1(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_u8(ctx, &out->id)) return bbq_fail(ctx, "NSubsec.id: read failed");
    BBQ_MUSTTAIL return jnm_n_subsec_k0(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k0(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_read_uleb128_u32(ctx, &out->size)) return bbq_fail(ctx, "NSubsec.size: read failed");
    BBQ_MUSTTAIL return jnm_n_subsec_k3(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k3(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_push_interval_checked(ctx, (size_t)(bbq_pos(ctx) + (out->size)))) return false;
    BBQ_MUSTTAIL return jnm_n_subsec_k5(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k5(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    out->body.tag = (int64_t)(out->id);
    switch ((int64_t)(out->id)) {
    case 0:
        BBQ_MUSTTAIL return jnm_n_subsec_k24(ctx, out, _struct_start);
    case 1:
        BBQ_MUSTTAIL return jnm_n_subsec_k22(ctx, out, _struct_start);
    case 2:
        BBQ_MUSTTAIL return jnm_n_subsec_k20(ctx, out, _struct_start);
    case 4:
        BBQ_MUSTTAIL return jnm_n_subsec_k18(ctx, out, _struct_start);
    case 10:
        BBQ_MUSTTAIL return jnm_n_subsec_k16(ctx, out, _struct_start);
    case 11:
        BBQ_MUSTTAIL return jnm_n_subsec_k13(ctx, out, _struct_start);
    default: BBQ_MUSTTAIL return jnm_n_subsec_k26(ctx, out, _struct_start);
    }
}
static bool jnm_n_subsec_k13(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_map_read(ctx, &out->body.u.case_5)) return bbq_fail(ctx, "NSubsec.body.u.case_5: NMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k16(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_indirect_map_read(ctx, &out->body.u.case_4)) return bbq_fail(ctx, "NSubsec.body.u.case_4: NIndirectMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k18(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_map_read(ctx, &out->body.u.case_3)) return bbq_fail(ctx, "NSubsec.body.u.case_3: NMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k20(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_indirect_map_read(ctx, &out->body.u.case_2)) return bbq_fail(ctx, "NSubsec.body.u.case_2: NIndirectMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k22(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_map_read(ctx, &out->body.u.case_1)) return bbq_fail(ctx, "NSubsec.body.u.case_1: NMap failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k24(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_module_name_read(ctx, &out->body.u.case_0)) return bbq_fail(ctx, "NSubsec.body.u.case_0: NModuleName failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k26(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_skip_read(ctx, &out->body.u.default_val)) return bbq_fail(ctx, "NSubsec.body.u.default_val: NSkip failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_n_subsec_k15(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k15(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    if (!bbq_pop_interval_checked(ctx)) return false;
    BBQ_MUSTTAIL return jnm_n_subsec_k28(ctx, out, _struct_start);
}
static bool jnm_n_subsec_k28(bbq_ctx_t* ctx, jnm_n_subsec_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
static bool jnm_name_data_k2(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    // Uncounted (eof/until) array: grow the backing store as elements parse.
    out->subsecs.count = 0; out->subsecs.items = NULL;
    bbq_push_loop_unbounded(ctx);
    if (bbq_pos(ctx) >= bbq_effective_end(ctx)) { bbq_pop_loop(ctx); BBQ_MUSTTAIL return jnm_name_data_k6(ctx, out, _struct_start); }
    out->subsecs.items = bbq_array_grow(ctx, out->subsecs.items, sizeof(*out->subsecs.items));
    if (!out->subsecs.items) return bbq_fail(ctx, "NameData.subsecs: alloc failed");
    out->subsecs.count = 1;
    BBQ_MUSTTAIL return jnm_name_data_k3(ctx, out, _struct_start);
}
static bool jnm_name_data_k3(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_push_scope(ctx, out);
    if (!jnm_n_subsec_read(ctx, &out->subsecs.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)])) return bbq_fail(ctx, "NameData.subsecs.items[bbq_loop_index_at(ctx, bbq_cur_loop_base(ctx) + 0)]: NSubsec failed");
    bbq_pop_scope(ctx);
    BBQ_MUSTTAIL return jnm_name_data_k5(ctx, out, _struct_start);
}
static bool jnm_name_data_k5(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    bbq_loop_advance(ctx);
    if ((bbq_pos(ctx) < bbq_effective_end(ctx))) {
        out->subsecs.items = bbq_array_grow(ctx, out->subsecs.items, sizeof(*out->subsecs.items));
        if (!out->subsecs.items) return bbq_fail(ctx, "NameData.subsecs: array: grow failed");
        out->subsecs.count = (size_t)(bbq_loop_index(ctx) + 1);
        BBQ_MUSTTAIL return jnm_name_data_k3(ctx, out, _struct_start);
    }
    bbq_pop_loop(ctx);
    BBQ_MUSTTAIL return jnm_name_data_k6(ctx, out, _struct_start);
}
static bool jnm_name_data_k6(bbq_ctx_t* ctx, jnm_name_data_t* out, size_t _struct_start) {
    (void)_struct_start; (void)ctx; (void)out;
    return true;
}
