/* AUTO-GENERATED by tools/opgen — do not edit. */
/* Source: tools/opgen/opcodes.def (JCVM 2.2.2 chapter 7). */
/*
 * adversarial_corpus.h — const-table layout for the
 * generated adversarial verifier-rejection corpus.
 */
#ifndef YOCTOJC_GEN_ADVERSARIAL_CORPUS_H
#define YOCTOJC_GEN_ADVERSARIAL_CORPUS_H

#include <stdint.h>
#include "yoctojc/types.h"

/* Method return type, resolved by the test runner into a
 * type_desc nibble sequence (JCVM §6.8.1 Table 6-6). VOID
 * means no descriptor (type_offset=0 — valid for opcodes
 * that don't do return-type checks). SHORT/INT/REF pick the
 * matching one-type descriptor for sreturn/ireturn/areturn
 * respectively. */
typedef enum {
    ADV_METHOD_VOID  = 0,
    ADV_METHOD_SHORT = 1,
    ADV_METHOD_INT   = 2,
    ADV_METHOD_REF   = 3,
} adv_method_ret_t;

typedef struct {
    const char*          name;
    uint8_t              opcode;
    const unsigned char* preamble_bc;
    uint16_t             preamble_len;
    verify_result_t      expected_error;
    uint8_t              max_stack;
    uint8_t              max_locals;
    uint8_t              method_ret;     /* adv_method_ret_t */
    uint8_t              access_flags;   /* vcap_add_method flags */
    uint8_t              acc_int;        /* CAP header acc_int bit (default 1) */
} adversarial_case_t;

extern const adversarial_case_t adversarial_cases[];
extern const int adversarial_case_count;
extern const int adversarial_opcode_count;

#endif
