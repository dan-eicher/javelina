# The HAND-WRITTEN ceiling. conformance/gates fails if the number of COVERED rows whose ONLY
# artifact is a hand-written class in conformance/jls RISES above it.
#
# Why this needs a gate of its own. The UNCOVERED ratchet cannot see this at all: a row carried
# by a hand-written program is COVERED, so it is subtracted from the number that is watched. 50
# rows sat in exactly that position while every gate stayed green, and on 2026-07-27 that let
# "chapter 4 closes" be said out loud while six of chapter 4's rows were carried by the class
# crisp-tallying-chapters §4 forbids:
#
#   "This applies to the corpus, not to a subset chosen at authoring time. I twice proposed
#    restricting stitching to the 'genuinely combinatorial' chapters and hand-writing the rest;
#    that is the same scope-narrowing as cutting the generator outright, and it is not an
#    authoring decision."
#
# And §5 says what to do about a rule nobody enforces: "A rule that cannot fire is not a gate --
# the same defect this project keeps finding in its own tests."
#
# It is not a hard zero, because the debt is real and cannot clear in one commit. It is a
# number that may only FALL. A row leaves the set when a GENERATED artifact claims its section,
# which is what migrating a chapter into templates does.
#
# THE TESTS THIS DISTRUSTS ARE NOT HYPOTHETICALLY BAD. §15.24 was COVERED by a hand-written
# Ch15 plus five generated cases and still had three wrong answers -- it rejected two legal
# programs and accepted one the spec calls a compile-time error -- because every one of those
# artifacts exercised the conditional operator and none exercised its TYPE RULE. §8.3.1.2,
# §6.6.2 and §6.4.2 were each the same shape: a section with a test, a green suite, and a rule
# nobody had read off the page.
#
# LOWER it by migrating a chapter's rows into templates written FROM THE SPEC. Porting the
# hand-written assertions forward does not count and is worse than leaving them: it launders
# the blind spot into the generator, which then certifies it.
#
# 34 -> 26 (this commit): eight of chapter 11's eleven, by conformance/gen/Lib11.java from
# pp.202-208. §11.5.1.1, §11.5.1.2 and §11.5.2 are NOT done and are named rather than implied:
# all three are LISTS -- the spec enumerates each exception class with the operation that
# throws it -- and a list is the one shape where sampling passes silently. They get one
# template per entry once pp.209-211 are transcribed, the way §3.9's 47 keywords did.
#
# The sharp one is §11.3: "if a finally clause is executed because of abrupt completion of a
# try block and the finally clause itself completes abruptly, then the reason for the abrupt
# completion of the try block is DISCARDED". A method that throws and whose finally returns 7
# returns 7 -- the exception is gone, so the caller's catch never runs.
#
# The NUL truncation recorded in the previous entry is FIXED, and the note it replaced is
# deleted rather than improved. java_source.h took the length as a parameter instead of
# recovering it with strlen; read_file already knew it from ftell and was discarding it.
#
# Its regression lives in compiler/test/test_parse.c, NOT here, and the reason is worth
# keeping: I first wrote a conformance template using a ` ` escape and it passed with the
# fix REMOVED. Six ASCII bytes on disk means strlen measures the whole file; the NUL only
# exists after §3.3 translation, where the length is already carried. Only a RAW NUL byte
# reproduces it, Emit gates generated source to ASCII and should, so the test needs an API
# that takes a buffer and a length. The conformance template stays, with its claim corrected
# to the narrower one it can make -- a NUL inside a comment is ordinary content.
#
# 42 -> 34 (previous): chapter 10 cleared, by conformance/gen/Lib10.java, written from
# pp.194-199 with Ch10.java and Ch10b.java unread.
#
# The two that a sampling test tends to miss, and why they are written the way they are:
#
#   §10.2  the spec's own `byte[] rowvector, colvector, matrix[];`, which it says "is
#          equivalent to `byte rowvector[], colvector[], matrix[][];`". The [] on the TYPE
#          distributes to every declarator; the [] on a declarator adds to that one alone. A
#          compiler that applied the trailing bracket to all three, or dropped it, still
#          COMPILES -- only indexing matrix twice tells them apart.
#   §10.7  an array's clone "overrides the method of the same name in class Object and throws
#          no checked exceptions". The template calls a.clone() with no try/catch and no throws
#          clause: had the array inherited Object.clone's signature, which declares
#          CloneNotSupportedException, the case would not compile at all.
#
# FOUND WHILE WRITING IT, unfixed and untested: javelinac SILENTLY TRUNCATES a source file at a
# NUL byte. A first draft of Lib10.java quoted §10.9's escape literally and so carried a real
# 0x00 in a comment -- legal Java, since §3.7 makes a comment's content any InputCharacter and
# §3.5 exempts only a trailing SUB. javelinac exited 0 having parsed the file up to that point,
# so `class Lib10` was never declared and the only symptom was "undefined 'Lib10'" reported
# against GenMain, a different file. A compiler accepting a file with exit 0 while dropping the
# half of it after byte N is a silent-truncation defect.
#
# CAUSE: java_source.h recovered the source length with strlen(). Not the grammar -- COMMENTS
# FROM "//" TO "\n" is fine -- and not pegc. read_file knew the true length from ftell and
# discarded it by returning a bare char*. FIXED in the next entry above.
#
# 50 -> 42 (previous): chapters 4 and 5 cleared. §4.3, §4.3.2, §4.3.3, §4.3.4, §4.5.2 and
# §4.5.5 by conformance/gen/Lib4Ref.java; §5.4 and §5.5 by Lib5Conv.java. Both written from the
# pages (p.37-47, p.67) with Ch4.java and Ch5.java deliberately unread -- porting the old
# assertions would launder whatever they missed into the generator, which would then certify it.
#
# What that buys beyond the count: §5.4's claim is the word ONLY ("string conversion applies
# only to the operands of the binary + operator when one of the arguments is a String"), and one
# expression cannot show it. The template prints `a + 2`, `"" + a + 2` and `a + 2 + ""` -- the
# same operands, three groupings, three different answers -- so an implementation that made a
# whole expression "stringy" once a String appeared anywhere would print one answer three times.
# That is the kind of claim a hand-written assertion tends to sample rather than pin.
#
# 50 (previous): the starting debt, across chapters 3, 4, 5, 10, 11, 14 and 15.
26
